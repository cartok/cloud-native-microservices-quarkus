services:
  db:
    image: docker.io/library/postgres:17
    ports:
      # Hat nicht funktioniert, disabled wieder Dev Service.
      # Dev Service verwendet Testcontainers, da hat man keinen Einfluss auf den Port.
      - "52520:5432"
    environment:
      POSTGRES_USER: quarkus
      POSTGRES_PASSWORD: quarkus
      POSTGRES_DB: quarkus
