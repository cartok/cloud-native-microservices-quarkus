quarkus:
  test:
    continuous-testing: PAUSED
  datasource:
    devservices:
      port: 52520
      reuse: true
      show-logs: true
  http:
    auth:
      # TODO: Somehow API was not automatically secured. Maybe cause I don't let the API be generated by extension.
      basic: true
